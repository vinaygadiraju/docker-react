sudo: required
language: generic 

services:
  - docker

before_install:
  - docker build -t vinaygadiraju/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true vinaygadiraju/docker-react npm run test